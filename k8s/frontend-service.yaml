apiVersion: v1
kind: Service
metadata:
  name: banking-frontend
  namespace: banking
spec:
  selector:
    app: banking-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
